# Interfaces

## Interfaces
+ methods are by default `public abstract`
+ methods marked als `default` are ...
+ record classes can implement an interface (they cannot extend an abstract class)
## Multiple inheritance

## Static fields

## the Singleton pattern
