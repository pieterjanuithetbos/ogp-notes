# Geneste klassen

1. static nested classes


2. inner classes
- is _nonstatic_: the inner class has an implicit `EnclosingClass.this` field that points to an instance of the enclosing class


3. local classes


4. anonymous classes
